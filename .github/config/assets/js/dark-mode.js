document.addEventListener('DOMContentLoaded', function() {
  // Permanently dark mode - toggle functionality removed per user request
  const isDarkMode = true;
  
  // No light/dark mode toggle added to keep permanent dark mode
});